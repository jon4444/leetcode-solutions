# from collections import Counter
class Solution:
    def numIdenticalPairs(self, nums: List[int]) -> int:
        freq = {}
        res = 0
        for num in nums:
            if num in freq:
                res += freq[num]
                freq[num] += 1
            else:
                freq[num] = 1
        return res
